type Stats {
  plays: String!
  tips: String!
}

type Query {
  stats: Stats
}

type Mutation{
  increasePlays(releaseId: String): Stats
}

type Subscription{
  playsIncreased: Stats,
  recentPlaysUpdated: Release
}