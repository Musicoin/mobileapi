type Stats {
  plays: String!
  tips: String!
}

type Query {
  stats: Stats
}

type Mutation{
  increasePlays(releaseId: String): Stats
  tip(musicoins: Int, trackAddress:String, email:String): Release
}

type Subscription{
  playsIncreased: Stats
}